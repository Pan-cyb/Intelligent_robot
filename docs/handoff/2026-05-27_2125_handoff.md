# 2026-05-27 21:25 Handoff

## Current Goal

将 BPU YOLO 视觉后端从“单节点验证”推进到整机集成验证，同时继续保持 `task_manager` 和 `follower_controller` 不大改。

## Important Context

- 长期规则见 `AGENTS.md`。
- 当前阶段状态见 `docs/PROJECT_STATUS.md`。
- 当前分支：`feature/bpu-person-tracker`。
- 用户已在 RDK X5 实机 runtime 中配置好 YOLO `.bin` 模型。
- 用户已验证 `person_tracker_bpu_node.py` 单节点可运行。
- 新视觉后端必须继续发布兼容 topic：

```text
/person_position
/person_distance
/fall_detected
```

- `follower_controller` 和 `task_manager` 原则上不需要因为 BPU 视觉后端而改代码。

## Completed This Session

- 读取并确认：
  - `AGENTS.md`
  - `docs/PROJECT_STATUS.md`
  - `docs/handoff/2026-05-25_2201_handoff.md`
- 记录用户新进展：
  - 实机 runtime 内 `.bin` 模型已配置。
  - `person_tracker_bpu_node.py` 单节点已验证。
- 更新 `docs/PROJECT_STATUS.md`，把下一步明确为整机集成验证。

## Changed Files

```text
docs/PROJECT_STATUS.md
docs/handoff/2026-05-27_2125_handoff.md
```

## Verification

本次只更新文档，未修改代码。

用户已在实机侧完成的验证：

```text
YOLO .bin 模型已配置在 runtime 中
person_tracker_bpu_node.py 单节点已验证
```

## Known Issues

- 还未验证 `robot_server.launch.py vision_backend:=bpu_yolo` 的整机启动链路。
- 还未验证 `follower_controller` 在 `FOLLOWING` 下使用 BPU 视觉输出是否平滑。
- 还未验证 `task_manager` 接收 BPU 节点 `/fall_detected` 后是否保持既有 EMERGENCY 行为。
- 如果整机 launch 传参没有带正确 `bpu_yolo_model_path`，BPU 节点会启动失败。

## Next Recommended Steps

1. 在 RDK X5 实机上构建并 source：

```bash
cd /home/pan/Intelligent_robot
colcon build --packages-select person_tracker task_manager follower_controller
source install/setup.bash
```

2. 用 BPU 后端启动整机服务端：

```bash
ros2 launch task_manager robot_server.launch.py \
  vision_backend:=bpu_yolo \
  bpu_yolo_model_path:=/path/to/runtime/model.bin
```

3. 另开终端观察视觉输出：

```bash
source /home/pan/Intelligent_robot/install/setup.bash
ros2 topic hz /person_position
ros2 topic echo /person_distance
ros2 topic echo /fall_detected
```

4. 观察 BPU 占用：

```bash
hrut_bpuprofile -b 0
cat /sys/devices/system/bpu/bpu0/ratio
```

5. 视觉输出稳定后，触发跟随任务：

```bash
ros2 service call /robot_server/start_task task_manager_interfaces/srv/StartTask \
  "{task_type: 'follow', target: '', text: ''}"
```

6. 跟随验证时观察：

```bash
ros2 topic echo /robot_mode
ros2 topic hz /person_position
ros2 topic echo /person_distance
```

7. 若整机链路正常，再把默认 demo 启动切到：

```bash
ros2 launch task_manager robot_server.launch.py \
  vision_backend:=bpu_yolo \
  bpu_yolo_model_path:=/path/to/runtime/model.bin \
  enable_demo_manager:=true \
  enable_rosa_always_listen:=true
```

## Resume Instructions

新会话继续时先读取：

```text
AGENTS.md
docs/PROJECT_STATUS.md
docs/handoff/2026-05-27_2125_handoff.md
```

然后优先做整机 `vision_backend:=bpu_yolo` 验证。只有当兼容 topic、TF、频率或深度稳定性暴露问题时，才小范围修改 `person_tracker_bpu_node.py` 或 launch 参数；不要先改 `task_manager` 和 `follower_controller`。
