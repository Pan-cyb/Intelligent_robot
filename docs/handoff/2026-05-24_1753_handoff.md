# 2026-05-24 17:53 Handoff

## Current Goal

准备最终综合 demo：保持 `task_manager` 只做单任务执行和状态机，新增独立 `demo_manager` 做多任务流程编排；调整语音、启动、视觉跟随和 TF 细节以支撑现场演示。

## Important Context

- 长期规则见 `AGENTS.md`。
- 当前阶段状态见 `docs/PROJECT_STATUS.md`。
- `task_manager` 不应承载整条 demo 流程，demo 流程由 `demo_manager` 独立节点编排。
- `demo_manager` 只调用 `/robot_server/start_task` 和 `/robot_server/query_robot_state` 等高层 service，不直接控制 Nav2 或 `/cmd_vel`。
- 最终 demo 当前不再自动触发 `inspection`，避免演示中抢占用户语音导航。
- `robot_server.launch.py` 当前默认启动相机、`person_tracker`、`follower_controller`，但默认不启动 RViz 或 `person_tracker` debug window。
- 本次环境里 `.git` 目录只读，无法由 Codex 执行 `git add/commit/push`；需要用户本机提交。

## Completed This Session

- 新增独立 `demo_manager` 包：
  - `src/demo_manager/demo_manager/demo_manager_node.py`
  - `src/demo_manager/launch/demo_manager.launch.py`
  - `src/demo_manager/doc/demo_manager_usage.md`
- `demo_manager` 当前流程：

```text
INIT
  -> WAKE_UP
  -> COMPANION_NAVIGATE
  -> COMPANION_DIALOGUE
  -> WAIT_FOR_FOLLOW_TRIGGER
```

- `demo_manager` 已移除自动 `inspection` 巡航阶段。
- `task_manager/launch/robot_server.launch.py` 支持可选启动：

```text
enable_demo_manager:=true
enable_rosa_always_listen:=true
separate_demo_terminals:=true
```

- `base_controller/launch/navigation.launch.py` 新增 `use_rviz` 参数，默认 `false`。
- `robot_server.launch.py` 默认：

```text
enable_person_tracker=true
enable_follower_controller=true
use_rviz=false
debug_window=false
```

- `rosa_agent/always_listen_voice_cli.py` 已支持：
  - 唤醒词“小智”，兼容“小志”。
  - 文本中包含唤醒词即可触发。
  - “小智去客厅”这种同句命令直接执行。
  - 唤醒后进入连续会话，`ASR_SESSION_IDLE_TIMEOUT_SEC=60` 后重新要求唤醒。
- `rosa_agent` 普通闲聊已从工具 agent 分流，闲聊不再主动查询 ROS 状态。
- `person_tracker` 修复：
  - `debug_window` 参数显式转 bool，避免 launch 传入字符串 `"false"` 被当作 True。
  - 无 debug window 时不调用 `cv2.destroyAllWindows()`。
  - `/person_position` 和 `/person_hand_position` 使用图像消息原始时间戳。
- `follower_controller` 修复：
  - 将 `/person_position` 转到 `map` 时使用最新 TF，避免 TF future extrapolation。
  - 发给 Nav2 的 follow goal 使用当前时间戳。
- 更新文档：
  - `AGENTS.md`
  - `docs/PROJECT_STATUS.md`
  - `src/demo_manager/doc/demo_manager_usage.md`

## Changed Files

```text
AGENTS.md
docs/PROJECT_STATUS.md
docs/handoff/2026-05-24_1753_handoff.md
src/base_controller/launch/navigation.launch.py
src/demo_manager/
src/follower_controller/scripts/follower_nav2_controller.py
src/person_tracker/scripts/person_tracker_node.py
src/task_manager/launch/robot_server.launch.py
src/rosa_agent/rosa_agent/always_listen_voice_cli.py
src/rosa_agent/rosa_agent/cli.py
src/rosa_agent/rosa_agent/voice.py
src/rosa_agent/rosa_agent/config.py
src/rosa_agent/.env.example
src/rosa_agent/doc/voice_usage.md
```

本地运行时配置文件也被修改但被 git 忽略：

```text
src/rosa_agent/.env
```

## Verification

已做过的验证包括：

```bash
python3 -m py_compile src/task_manager/launch/robot_server.launch.py
python3 -m py_compile src/person_tracker/scripts/person_tracker_node.py src/follower_controller/scripts/follower_nav2_controller.py
python3 -m py_compile src/demo_manager/demo_manager/demo_manager_node.py src/demo_manager/launch/demo_manager.launch.py
python3 -m py_compile src/base_controller/launch/navigation.launch.py
colcon build --packages-select base_controller demo_manager task_manager
colcon build --packages-select task_manager
```

需要用户在目标运行工作区重新完整 build/source，尤其注意实际路径可能是 `/home/sunrise/Myproj` 而不是 `/home/pan/Intelligent_robot`。

## Known Issues

- 用户运行日志显示旧版 `demo_manager` 仍出现：

```text
WAIT_FOR_INSPECTION_TIMER
INSPECTION
Request task: type=inspection
```

但当前 `/home/pan/Intelligent_robot/src/demo_manager` 和 `install/demo_manager` 已无 inspection 阶段。高度怀疑实际运行 workspace/source 不是当前工作区，或旧进程未停止。

- `person_tracker` 曾因无 display 且 `debug_window` 被当 True 导致 Qt/xcb 崩溃，已修复源码，但需要重新 build/source。
- `follower_controller` 曾出现 `camera_link -> map` TF future extrapolation，已改为使用最新 TF，但仍需实机复测。
- 当前 `.git` 目录只读，Codex 无法执行 commit/push。

## Next Recommended Steps

1. 确认实际运行工作区：

```bash
ros2 pkg prefix demo_manager
python3 -c "import demo_manager.demo_manager_node as m; print(m.__file__)"
grep -R "WAIT_FOR_INSPECTION_TIMER\\|auto_inspection\\|start_task(\"inspection" $(ros2 pkg prefix demo_manager) -n
```

2. 在实际运行工作区重新编译并 source：

```bash
cd /home/pan/Intelligent_robot
colcon build --packages-select base_controller task_manager demo_manager rosa_agent person_tracker follower_controller
source install/setup.bash
```

如果实机路径是 `/home/sunrise/Myproj`，必须在该路径执行。

3. 轻量综合 demo：

```bash
ros2 launch task_manager robot_server.launch.py enable_demo_manager:=true enable_rosa_always_listen:=true
```

4. 三终端调试方式：

```bash
# Terminal 1
ros2 launch task_manager robot_server.launch.py

# Terminal 2
ros2 launch demo_manager demo_manager.launch.py

# Terminal 3
ros2 run rosa_agent rosa_always_listen
```

5. 如需完全关闭视觉跟随：

```bash
ros2 launch task_manager robot_server.launch.py enable_person_tracker:=false enable_follower_controller:=false
```

6. 如需 RViz 或 debug window：

```bash
ros2 launch task_manager robot_server.launch.py use_rviz:=true
ros2 launch task_manager robot_server.launch.py debug_window:=true
```

7. 用户本机提交：

```bash
git add AGENTS.md docs/PROJECT_STATUS.md docs/handoff/2026-05-24_1753_handoff.md \
  src/base_controller/launch/navigation.launch.py \
  src/demo_manager \
  src/follower_controller/scripts/follower_nav2_controller.py \
  src/person_tracker/scripts/person_tracker_node.py \
  src/task_manager/launch/robot_server.launch.py \
  src/rosa_agent/rosa_agent/always_listen_voice_cli.py \
  src/rosa_agent/rosa_agent/cli.py \
  src/rosa_agent/rosa_agent/config.py \
  src/rosa_agent/rosa_agent/voice.py \
  src/rosa_agent/.env.example \
  src/rosa_agent/doc/voice_usage.md

git commit -m "Prepare final lightweight demo orchestration"
git push origin main
```

## Resume Instructions

新会话继续时先读取：

```text
AGENTS.md
docs/PROJECT_STATUS.md
docs/handoff/2026-05-24_1753_handoff.md
```

然后优先确认实际运行 workspace 与源码 workspace 是否一致，避免继续排查已修复但未安装的旧代码。
