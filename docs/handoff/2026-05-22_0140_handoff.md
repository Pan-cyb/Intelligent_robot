# 2026-05-22 01:40 Handoff

## Current Goal

把系统改成 “ROSA 调用高层任务接口，robot 服务端维护状态机和执行流程”。

## Important Context

- 长期架构规则见 `AGENTS.md`。
- 当前阶段状态见 `docs/PROJECT_STATUS.md`。
- `task_manager` 是任务执行入口；ROSA / LLM 只做自然语言理解和高层工具选择。
- LLM 不应直接调用 Nav2、`/cmd_vel` 或拼接底层动作序列。

## Completed This Session

- 创建 git 分支：`rosa-high-level-task-interface`。
- 新增接口包 `task_manager_interfaces`。
- 新增 `/robot_server/start_task`，支持 `wake_up`、`navigate`、`speak`。
- 新增 `/robot_server/start_wakeup_task`、`/robot_server/cancel_current_task`、`/robot_server/query_robot_state`。
- 保留旧 `/trigger_wakeup_task`、`/cancel_task`、`/task_command` 兼容入口。
- `task_manager` 现在集中维护状态转换：
  - `wake_up`: `IDLE -> SCHEDULED_TASK -> NAVIGATION -> CONVERSATION -> IDLE`
  - `navigate`: `IDLE -> NAVIGATION -> IDLE`
  - `speak`: `IDLE -> CONVERSATION -> IDLE`
- ROSA 默认 action tools 已收敛为高层工具：
  - `start_wakeup_task`
  - `navigate_to_named_place`
  - `speak_text`
  - `cancel_current_task`
  - `query_robot_state`

## Changed Files

- `src/task_manager_interfaces/`
- `src/task_manager/task_manager/task_manager_node.py`
- `src/task_manager/package.xml`
- `src/rosa_agent/rosa_agent/action_tools.py`
- `src/rosa_agent/package.xml`
- `src/task_manager/doc/robot_server_usage.md`
- `docs/PROJECT_STATUS.md`

## Verification

已通过：

```bash
python3 -m py_compile src/task_manager/task_manager/task_manager_node.py src/rosa_agent/rosa_agent/action_tools.py
source /opt/ros/humble/setup.bash && colcon build --packages-select task_manager_interfaces task_manager rosa_agent
```

## Known Issues

- 尚未在真实机器人 / Nav2 运行态下做端到端 service 调用验证。
- `speak` 任务通过发布 `/tts_text` 后等待 `tts_wait_sec` 估算播报完成；当前 TTS 节点没有完成回执。

## Next Recommended Steps

1. 启动 `ros2 launch task_manager robot_server.launch.py`。
2. 手动调用 `/robot_server/start_task` 测试 `wake_up`、`navigate`、`speak`。
3. 导航过程中再调用普通任务，确认返回 busy；调用 cancel 确认取消 Nav2 goal。
4. 从 ROSA 语音入口触发 `start_wakeup_task()` 做联调。

## Resume Instructions

新会话继续时先读取：

```text
AGENTS.md
docs/PROJECT_STATUS.md
docs/handoff/2026-05-22_0140_handoff.md
```

然后直接进入端到端运行验证或根据测试结果修正状态机。
