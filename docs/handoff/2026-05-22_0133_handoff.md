# Handoff 2026-05-22 01:33

## Current Goal

最小闭环已经完成。当前可运行链路：

```text
命令端发任务
  -> task_manager 接收任务
  -> 读取 bedroom_bedside
  -> Nav2 导航
  -> 到达后发布 /tts_text
  -> rosa_agent tts_node 播放语音
  -> task_manager 回到 IDLE
```

下一阶段建议进入“语音命令接入任务调度”。

## Important Context

先读：

```text
AGENTS.md
docs/PROJECT_STATUS.md
```

长期规则仍在 `AGENTS.md`。阶段进度已更新到“最小闭环已完成”。

通用交接 skill：

```text
/home/pan/.codex/skills/project-context-handoff/SKILL.md
```

## Completed This Session

确认并记录：

```text
最小闭环已完成
服务端入口：ros2 launch task_manager robot_server.launch.py
命令端入口：/trigger_wakeup_task 或 /task_command
任务目标：bedroom_bedside
到达后通过 /tts_text 触发 rosa_agent tts_node 播报
```

更新：

```text
docs/PROJECT_STATUS.md
docs/handoff/2026-05-22_0133_handoff.md
```

## Changed Files

```text
docs/PROJECT_STATUS.md
docs/handoff/2026-05-22_0133_handoff.md
```

## Verification

用户确认当前最小闭环已经完成。本次只更新上下文文档，没有重新运行 ROS 构建或启动命令。

## Known Issues

自然语言语音输入尚未正式接入 `task_manager`。当前任务触发仍是：

```bash
ros2 service call /trigger_wakeup_task std_srvs/srv/Trigger {}
```

或：

```bash
ros2 topic pub --once /task_command std_msgs/msg/String "{data: wakeup_bedroom}"
```

## Next Recommended Steps

1. 设计语音命令到任务名的映射方式。
2. 让 `rosa_agent` 或一个轻量语音命令节点把识别结果发布到 `/task_command`。
3. 先支持固定任务名：

```text
wakeup_bedroom
go_charge
go_kitchen
go_livingroom_sofa
```

4. 再考虑用 LLM 做自由语言到规范任务名的解析。
5. 增加更多 `task_manager` 模块化任务。

## Resume Instructions

需要直接继续干活时：

1. 读取 `AGENTS.md`。
2. 读取 `docs/PROJECT_STATUS.md`。
3. 读取本 handoff。
4. 直接进入“语音命令接入 `/task_command`”或“新增更多任务登记”的实现。
