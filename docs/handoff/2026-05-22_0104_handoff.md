# Handoff 2026-05-22 01:04

## Current Goal

项目当前目标是跑通养老陪伴机器人最小任务闭环：

```text
命令端发任务
  -> task_manager 接收任务
  -> 读取 bedroom_bedside 等命名点
  -> Nav2 导航
  -> 到达后发布 /tts_text
  -> rosa_agent tts_node 播放语音
  -> task_manager 回到 IDLE
```

## Important Context

先读：

```text
AGENTS.md
docs/PROJECT_STATUS.md
```

长期规则在 `AGENTS.md`，阶段进度在 `docs/PROJECT_STATUS.md`。

本次还创建了通用 Codex skill：

```text
/home/pan/.codex/skills/project-context-handoff/SKILL.md
```

该 skill 不绑定本项目，适用于任何项目的上下文维护和会话交接。

## Completed This Session

新增顶层项目上下文：

```text
AGENTS.md
docs/AGENT.md
docs/PROJECT_STATUS.md
```

创建通用 skill：

```text
/home/pan/.codex/skills/project-context-handoff/SKILL.md
```

确认当前项目关键状态：

```text
robot_server.launch.py 已作为完整服务端入口
task_manager 当前叫醒任务目标是 bedroom_bedside
named_locations.yaml 已使用真实标定点
task_manager 已兼容 x/y/yaw 和 position/orientation 两种地点格式
```

## Changed Files

项目内：

```text
AGENTS.md
docs/AGENT.md
docs/PROJECT_STATUS.md
docs/handoff/2026-05-22_0104_handoff.md
```

Codex 本地 skill：

```text
/home/pan/.codex/skills/project-context-handoff/SKILL.md
```

## Verification

已读取并检查：

```text
AGENTS.md
docs/PROJECT_STATUS.md
/home/pan/.codex/skills/project-context-handoff/SKILL.md
```

本次主要是文档和 skill 配置更新，没有重新构建 ROS 包。

## Known Issues

自然语言语音输入尚未完整接入 `task_manager` 的 `/task_command`。当前最小闭环仍以命令端手动发布任务为主：

```bash
ros2 service call /trigger_wakeup_task std_srvs/srv/Trigger {}
```

或：

```bash
ros2 topic pub --once /task_command std_msgs/msg/String "{data: wakeup_bedroom}"
```

## Next Recommended Steps

1. 新会话先读 `AGENTS.md`、`docs/PROJECT_STATUS.md` 和本 handoff。
2. 在真实机器人上启动服务端：

```bash
cd /home/pan/Intelligent_robot
source install/setup.bash
ros2 launch task_manager robot_server.launch.py
```

3. 命令端触发任务：

```bash
ros2 service call /trigger_wakeup_task std_srvs/srv/Trigger {}
```

4. 验证机器人导航到 `bedroom_bedside`，到达后 TTS 正常播报。
5. 如果闭环通过，下一步把语音理解结果接到 `/task_command`。

## Resume Instructions

需要直接继续干活时：

1. 读取 `AGENTS.md`。
2. 读取 `docs/PROJECT_STATUS.md`。
3. 读取最新的 `docs/handoff/*_handoff.md`。
4. 直接继续实现下一步，不需要重新解释背景。
